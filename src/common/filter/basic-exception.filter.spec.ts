import { BasicExceptionFilter } from './basic-exception.filter';

describe('BasicExceptionFilter', () => {
  it('should be defined', () => {
    expect(new BasicExceptionFilter()).toBeDefined();
  });
});
